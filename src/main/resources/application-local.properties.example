# Copy to application-local.properties (gitignored) and fill in your local MySQL credentials.
# Create database: CREATE DATABASE reviewflow_dev;
# Create user: CREATE USER 'reviewflow_user'@'localhost' IDENTIFIED BY 'your_password';
# Grant: GRANT SELECT, INSERT, UPDATE, DELETE, CREATE, DROP, INDEX, ALTER ON reviewflow_dev.* TO 'reviewflow_user'@'localhost';
# FLUSH PRIVILEGES;

spring.datasource.url=jdbc:mysql://localhost:3306/reviewflow_dev
spring.datasource.username=root
spring.datasource.password=olamide588
